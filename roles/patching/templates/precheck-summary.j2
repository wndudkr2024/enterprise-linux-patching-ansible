Host: {{ inventory_hostname }}
Distribution: {{ ansible_distribution }} {{ ansible_distribution_version }}
Kernel before patching: {{ patching_pre_patch_kernel.stdout }}
Uptime before patching: {{ patching_pre_patch_uptime.stdout }}
Security-only mode: {{ patching_security_only }}
Excluded packages: {{ patching_excluded_packages | join(', ') }}
